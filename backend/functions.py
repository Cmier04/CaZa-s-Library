# Define functions needed for program
# comments to explain what each function does (one to three words)

import json
import os

def load_users():
    'load users from users.json'
    #TODO: open the file and check if the file exists, return info

def save_users(users):
    'saves the users to users.json'
    #TODO: implement the function by opening the file and writing in it

def authenticate()